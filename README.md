# 话题守卫 topic_guard

自动检测对话话题是否结束并主动停止对话。支持双机器人互聊与人机对聊。

## 指令
- /tstop（话题停止）：手动结束当前话题
- /tresume（话题恢复）：恢复对话
- /tstatus（话题状态）：查看会话状态
- /treset（话题重置）：重置话题与历史
- /tstat（话题统计）：查看统计

## 原理
关键词仅触发 LLM 判定，由 LLM 判断话题是否自然结束；
连续多次判定结束且结束倾向 EMA 超阈值后停止；
停止后通过 @、问句、关键词、私聊长消息自动恢复。
